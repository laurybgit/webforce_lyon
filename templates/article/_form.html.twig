{{ form_start(form) }}
{# {{ form_widget(form) }} #}
<div class="form-group">

	{{ form_row(form.description, {'label': 'Description','attr': {'class': 'form-control', 'type':'text'}}) }}
</div>
<div class="form-group">

	{{ form_row(form.content, {'label': 'Content','attr': {'class': 'form-control', 'type':'text'}}) }}
</div>
<div class="form-group">

	{{ form_row(form.imageFile, {'label': 'Image','attr': {'class': 'form-control-file', 'type':'file'}}) }}
</div>

<div class="form-group">
	{{ form_row(form.title, {'label': 'Title','attr': {'class': 'form-control', 'type':'text'}}) }}

</div>

<button class="btn btn-primary">{{ button_label|default('Save') }}</button>
{{ form_end(form) }}
